# Roadmap: Aurora v1.8 Web UI Reliability

**Status:** In progress
**Milestone:** v1.8 Web UI Reliability
**Started:** 2026-08-23

## Overview

v1.7 (Web UI Parity) rebuilt the voting page and dashboard against the real
REST API, and fixed the response-DTO JSON casing (snake_case) to match the
documented convention. v1.8 hardens that work so the Web UI cannot silently
regress again and the coverage bar held by every other deliverable also
applies to the browser surface.

## Success Criteria

- [x] Web UI ↔ REST API **contract test** parses every `/api/v1/...`
      reference (with method) from `web/*.html` + `web/js/app.js` and asserts
      each resolves to a registered chi route (8c262db) — guards against the
      silent-404 regression v1.7's audit caught (`/voting/create` → 404).
- [x] Voting page performs real actions (register voter/candidate, create
      session, list sessions, list candidates, cast signed vote) (c014200).
- [x] Dashboard "Recent Activity" panel wired to real lottery + voting APIs;
      lottery history date column fixed (80e4353).
- [x] Dashboard "Total Votes" card now populated from real candidate
      `vote_count` data (previously the bound `stats.votes` was never set).
- [x] HTTP smoke test boots the real API (temp SQLite + applied migrations)
      and drives the full voting flow over HTTP — register voter/candidate,
      create session, list sessions, cast signed vote, tally, double-vote
      rejection (guards the Web UI's exact API contract).
- [x] Full `go test -race ./...` green including contract + smoke tests.

## RIL Tasks

| Task | Status | Evidence |
| ---- | ------ | -------- |
| TASK-fix-web-voting-page | resolved | c014200 |
| TASK-001 (dashboard recent activity) | resolved | 80e4353 |
| TASK-002 (web UI ↔ API contract test) | resolved | 8c262db |
| ISS-002 (endpoint-drift guard) | resolved | 8c262db |
| TASK-003 (voting HTTP smoke test) | active | — |
| TASK-004 (dashboard total votes stat) | active | — |

## Next

- Consider exposing an aggregate NFT/Token count endpoint, or drop the
  "Total Lotteries" / "Total Votes" distinction if data sources shift.
- Extend the HTTP smoke test to boot an actual `httptest.Server` and load the
  served HTML/JS to confirm the Web UI assets render with the injected API key.
