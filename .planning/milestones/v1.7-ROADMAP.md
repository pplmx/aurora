# Roadmap: Aurora v1.7 Web UI Parity

**Status:** Complete ✅
**Milestone:** v1.7 Web UI Parity
**Started:** 2026-08-22

## Overview

Make the documented browser Web UI actually functional against the real
voting API. An audit of `web/` found the voting page is a facade:
`web/js/app.js` `createVote()` POSTs to `/api/v1/voting/create` — which is not
a registered route (404) — and `castVote()` is an `alert()` stub. The page's
model (title / owner / yes-no) does not match the real voting domain
(candidates, sessions with candidate rosters, Ed25519-signed ballots). The
audit also surfaced an API gap: sessions could be created but never listed.

## Success Criteria

- [x] `GET /api/v1/voting/sessions` lists sessions (repo `ListSessions()` was
      already implemented; added use case + handler + tests) (00ab212)
- [x] `web/voting.html` / `web/js/app.js` use only real API endpoints and
      perform real actions (register voter/candidate, create session, list
      sessions, list candidates, cast a signed vote) (c014200)
- [x] Voting response DTOs emit snake_case JSON matching the documented
      convention and request bodies (PublicKey → public_key, etc.) (c014200)
- [x] Dashboard Recent Activity panel wired to real lottery + voting APIs;
      lottery history date column fixed (timestamp, not created_at) (80e4353)

## RIL Tasks

| Task | Status | Evidence |
| ---- | ------ | -------- |
| TASK-add-list-sessions-api | resolved | 00ab212 |
| TASK-fix-web-voting-page | resolved | c014200 |
| TASK-001 (dashboard recent activity) | resolved | 80e4353 |

## Next

- Add automated web UI coverage / contract tests so the voting page endpoint
  usage stays aligned with the API (and won't silently 404 again).
