# Roadmap: Aurora v1.11 Lifecycle & Parity Coverage

**Status:** Complete ✅
**Milestone:** v1.11 Lifecycle & Parity Coverage
**Started:** 2026-08-23

## Overview

v1.9–v1.10 closed API hardening and identity/authenticity gaps (ballot forgery,
NFT key↔owner binding, voting session lifecycle endpoints). v1.11 hardens the
*verification* of that surface: drive the newly-added session lifecycle and the
ended-session enforcement end-to-end through the real HTTP stack so regressions
in the parity/security work are caught by the suite, not by a misbehaving live
deployment.

## Success Criteria

- [x] Extend the real-API voting smoke test to drive the session lifecycle
      over HTTP: create → vote → **end** (vote rejected on ended session) →
      **start** (fresh voter can vote) — proving the lifecycle endpoints and
      the v1.10 ended-session enforcement hold end-to-end.
- [x] Standardize token/NFT response DTOs to snake_case JSON
      (`token_id`, `block_height`, …), matching the v1.7 voting standard and
      the API design doc (they previously emitted PascalCase).
- [x] Add real-API smoke coverage for token `approve`/`allowance` (incl.
      mismatched-key rejection) and NFT forged-transfer rejection (KEY_MISMATCH)
      under the live HTTP stack.
- [x] Full `go test -race ./...` green with the extended smoke tests.

## RIL Tasks

| Task | Status | Evidence |
| ---- | ------ | -------- |
| TASK-012 (voting lifecycle E2E smoke coverage) | resolved | 95ada74 |
| TASK-013 (token/NFT response DTO snake_case parity) | resolved | — |
| TASK-014 (token approve/allowance + NFT key-binding smoke) | resolved | — |

## Next

- Final v1.11 audit: all tasks resolved, race green, graph consistent.
